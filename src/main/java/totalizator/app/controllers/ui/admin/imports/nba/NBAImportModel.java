package totalizator.app.controllers.ui.admin.imports.nba;

import totalizator.app.controllers.ui.AbstractPageModel;
import totalizator.app.models.User;

public class NBAImportModel extends AbstractPageModel {

	protected NBAImportModel( final User currentUser ) {
		super( currentUser );
	}
}
